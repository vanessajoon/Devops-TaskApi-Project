services:
  web:
    build: 
      context: ..
      dockerfile: Docker-files/Dockerfile
    ports:
      - "8000:5000"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ..:/app
    depends_on:
      - redis

# redis name
  redis: 
    image: "redis:7-alpine"